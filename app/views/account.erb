<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <a href="/">Log Out</a>
<% if Helper.is_logged_in?(session) %>
<h2>Welcome, <%=  Helper.current_user(session).username %></h2>
<h3>Current Balance: <%= Helper.current_user(session).balance %></h3>
<a href="/logout">Logout</a>
<% else %>
<h2>You must <a href="/">Log In</a>to view youe balance</h2>
<% end %>
  </body>
</html>